#define CATCH_CONFIG_MAIN
#include "UnitTest.h"
#include "GameTool/Math2D.h"

TEST_CASE("Sanity checks :)", "[Sanity]") {
	REQUIRE(1 == 1);
	REQUIRE(2 != 1);
}

TEST_CASE("Math2D checks :)", "[Sanity]") 
{

}